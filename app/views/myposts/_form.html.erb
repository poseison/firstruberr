<%= form_for(@mypost) do |f| %>
  <% if @mypost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mypost.errors.count, "error") %> prohibited this mypost from being saved:</h2>

      <ul>
      <% @mypost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mytext %><br>
    <%= f.text_field :mytext %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
